# ColibreIntroPaper

```
git submodule update --init --recursive
cd velociraptor-comparison-data
./convert.py [--nproc <NUMBER OF PARALLEL PROCESSES>]
cd ..
```

Contains `phase_diagrams.diff` and `plot_gravitational_instability.diff`, which were last minute changes in order to get the response out. I don't currently have time to look through them to see what I changed.
